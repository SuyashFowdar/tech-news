<%= form_with(model: article, local: true) do |form| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <% @categories.each do |category| %>
    <div class="row margin8 invitation-item">
      <%= check_box("", "categories", {:multiple => true}, category.id) %>
      <%= category.name %>
      <%
=begin%>
 <div class="margin8 row cross-center w-25"> <%= category.name %> </div>
      <%= link_to 'Add Category', class: "links margin8", :action => 'create_article_category', :article => @article, :category => category %> 
<%
=end%>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
